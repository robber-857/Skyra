# 开发交接：课程类型隔离 → 开发店交易验收 → Customer Profile

更新：2026-09-15。本文件是当前续开发入口；历史实现细节见 [2026-09-14 交接](handoff-2026-09-14.md)。

## 当前状态

- 分支：`bookingdev`；本批开始时本地与 `origin/bookingdev` 都位于 `6ee83fc`，工作区干净。
- 用户已确认 Render 的 Checkout 与 Existing Pass 状态均为 `open`，并已在 Skyra Booking Settings 点击 `Enable development bookings`。这是用户操作确认；还没有用订单、Receipt、Outbox 和 Booking 记录签收真实交易。
- 本轮只读检查 `https://skyra-booking-web.onrender.com/health` 返回 200。开发店 Home/Programs 仍受 storefront password 保护，不能用匿名 HTTP 请求代替顾客登录或测试付款。
- 提交 `49bb1d9be78edfd2d0da2cfdd4a9be3f4ebc2596` 已推送，远程 SHA 一致；GitHub CI 34859591792 success。Render 已切换到新 Admin 资源且 `/health` 为 200。
- Shopify CLI 配置验证通过；未发布候选版本 `skyra-booking-6` 已创建。Release 会影响该 App 的所有安装店铺，因此被保留为待明确批准动作，当前 Shopify 前台扩展仍是上一已发布版本。
- 正式店没有开启任何开发门控，也没有执行正式营业发布。

## 本批完成

1. `CLASS` 普通团课、`APPOINTMENT` 私教、`COURSE` Workshop 全部进入 Classes & Passes、Weekly Schedule 和公开 Booking flow。
2. Admin 与前台课程卡/详情显示明确类型；私教容量固定为 1，Workshop 使用配置容量。
3. 一个 Pass 的 Eligible classes 只能来自同一种 Service kind。应用保存时返回 `PASS_TYPE_MISMATCH`，数据库也拒绝绕过应用层的混合类型写入。
4. 已关联 Session 或 Pass eligibility 的 Service 不能直接更改 kind，避免历史预约或已售 Pass 被重新解释。
5. 新增迁移 `202609150012_service_type_pass_isolation`；测试库现有 13 条迁移。

## 验证证据

- 定向：2 文件 / 48 项测试通过，覆盖 Workshop 公开 attempt/Pass、Workshop 排课、私教容量、应用与数据库双层跨类型拒绝。
- 全量：28 文件 / 350 项测试通过。
- `npm.cmd run check`、Customer Extension typecheck、ESLint、production build、`prisma validate`、`git diff --check` 通过。
- Home/Programs 16 组 320/390/430/1440px fixture 浏览器回归通过；无横向溢出，登录、恢复、Drop-in、已有 Pass 流程未回归。
- 上述 fixture 和自动化不等于 Shopify 真实账户或测试订单 E2E。

## 仍未完成

- 明确批准并 release Shopify App 候选版本 `skyra-booking-6`，再核对开发店前台扩展。
- 第一笔 Group class Drop-in：选课 → 顾客登录 → Shopify 测试 Checkout → `orders/paid` → Worker → Entitlement/Booking confirmed。
- 新 Group class Pass 测试付款；付款后使用发放的 Pass 预约另一节符合资格团课。
- 私教与 Workshop 测试商品/未来排期、各自 Pass 付款/已有 Pass 正向测试，以及团课 Pass 预约私教/Workshop 的负向拒绝验收。
- Customer Account Profile 可选个人签名/训练目标。
- 真实邮件 provider/收件人、Webhook reconciliation、异常人工恢复、正式店安装和生产支付。

## 下一步顺序

1. 用户明确批准后 release `skyra-booking-6`，刷新开发店 Home/Programs 核对前台 Workshop 类型展示。
2. 由顾客本人在开发店完成第一笔 Group class Drop-in 测试付款。只有 Shopify Order、Webhook Receipt/Outbox、Worker 结果和 Booking 状态可作为通过证据。
3. 完成新 Group class Pass 付款和已有 Pass 预约。
4. 录入私教与 Workshop 测试目录/排期，完成正向交易和错误类型拒绝。
5. 实现 Customer Profile 可选个人签名/训练目标，不扩大 Shopify Customer 写权限。

操作与紧急关闭见 [开发店三个预约开关](development-store-release.md)，完整剩余范围见 [开发状态](development-status.md) 与 [Backlog](implementation-backlog.md)。
