# Booking V3 开发交接 — 2026-09-12

## 新会话从这里开始

继续 D:\Skyra 的 Booking System V3。先读本文件、development-status.md、implementation-backlog.md 和 booking-app/AGENTS.md，再检查当前 Git/进程状态。用户要求继续开发，但网络很慢：不要连续刷新 Shopify、重复执行联网诊断或自动批量重启服务。本轮完成本地交接与提交，GitHub 推送暂缓。用户已授权推送 robber-857/Skyra 的 bookingdev 分支；网络恢复后可继续，无需再次索取同一授权。

## 不变的业务范围

- Home / Programs 使用同一个 Theme App Extension 组件和业务流程；参考图仅用于区块内下单，不用于 Customer Account 个人中心。
- Find a Class → Details → Shopify 登录校验/恢复 → Select a Pass（含 Drop-in）→ Review → 交易/结果，均保留当前 section。只有 Shopify 登录与原生 Checkout 外部交接。
- 未登录点击 Book 出现 Skyra 提示，桌面打开 Shopify 官方登录窗口；手机/弹窗受限时同页登录。不存在第二套客户密码；返回后重新验证签名 App Proxy 身份。
- 已有 Pass 应直接原子确认并扣次，不进入 A$0 Checkout。新 Pass / Drop-in 应在 Review 确认后建立 15 分钟 Hold，再进入 Shopify Checkout。
- Admin 定义课程/Pass 名称、价格等，Worker 同步稳定的 Shopify Product/Variant；每周每一场 Session 只存 Booking DB，不重复创建商品。
- 不引入 Mindbody 品牌、独立 Cart/Details/Pass 页面或自建支付表单。

## 本次已完成

1. 共享 UI：Home 桌面窄栏父容器已修复，采用 Programs 的视觉结构；双入口同组件，桌面课表/双栏摘要，手机单列/可折叠 Booking Details。
2. Calendar：31 天 Full Calendar、月份与前后七天导航、跨月/边界，返回课表恢复筛选、日期和 Book 焦点。
3. Attempt / Recovery：签名 Shopify 身份、服务器 opaque token、登录后绑定、网络错误保留 token；过期/满员/跨账号恢复、重新登录、Pass 变化重新选择。
4. 新 Pass / Drop-in：服务端返回已同步且价格/版本一致的可选商品；Review 再校验价格与容量。Drop-in 的 Service 从 Attempt 对应 Session 推导，不接收客户端价格/Variant/Service。
5. Drop-in UI 使用 kind:id 选择键，避免和 Pass ID 冲突；支持价格刷新、Edit、商品不可用恢复。Checkout 仍禁用。
6. Programs Shopify Page 已创建：167140557092，handle 与 templateSuffix 均为 programs；HTTP 200 且存在 Booking mount 已验证。历史文档中的 Page 404 / 内容授权等待问题已解除，不要重复创建。
7. 预览脚本固定开发店/开发主题；App 启动显式传 --path，避免从仓库根目录启动时找不到 toml。Theme 排除原有 assets/skyra.css，使用 --nodelete。
8. Docker 恢复后 PostgreSQL、Redis、App 和 Worker 已启动；启动日志显示无待执行迁移。进程状态属于交接时快照，新会话需重新检查。

## 502 的证据与边界

捕获 Node 请求 AggregateError：IPv4 250ms ETIMEDOUT，随后 IPv6 ENETUNREACH；2 秒连接竞速窗口仍不足。改为 IPv4 优先、关闭地址族竞速后，10 次独立连接均成功，但这不证明最终页面长期稳定；也观察到上游 503。

开发脚本只对子进程设置 NODE_OPTIONS 的 --dns-result-order=ipv4first --no-network-family-autoselection；诊断脚本设置同等 Node API。不修改系统 IPv6、TLS 或生产运行环境。参数依据：https://nodejs.org/api/cli.html#--no-network-family-autoselection 。用户反馈网络卡后已停止连续真实页面测试。

## 仍未完成（不要标成已交付）

- 真实 Shopify 顾客登录/退出/弹窗返回与手机同页返回的完整双入口 E2E；fixture 不能代替真实账号。
- 最终商品渠道发布/可售资格校验。当前 DB 映射检查不等于 Shopify Checkout 可购买保证。
- 公开 Review → Hold → Shopify Cart / Checkout。内部 Hold 已有锁与过期机制，但模型仍强制 PassPlan 外键，不能用于 Drop-in；需先调整模型与迁移。
- Entitlement grant/reserve/consume/release 追加式权益台账、已有 Pass 余额/有效期/服务匹配、Intro 资格。
- orders/paid 幂等处理、支付归属与金额/商品校验、权益生成、预约确认；过期 Hold 后付款与重复/乱序 Webhook 的恢复。
- 已有 Pass 原子确认、CONFIRMED / Confirmation、付款后 Recovery / Needs Attention。
- Customer Account 完整个人中心、Coach Portal/签到/留言、完整报表/通知/迁移、托管部署及上线。

保持 onlineBookingsEnabled=false，checkoutAvailable=false、ownedPassesAvailable=false，直至相应交易路径完成验收。本轮没有收款、扣课或真实预约确认，也没有部署正式店。

## 新会话建议执行顺序

1. 读 Git 状态和本地服务日志，保留无关改动；网络恢复后只做一次必要健康诊断，再针对失败项处理。
2. 验证 Home / Programs 真实桌面与手机流程，完成 Shopify 登录/退出/返回；检查已有 Programs Page，不重复创建。
3. 确认本地交接提交后推送 bookingdev，读取远程 SHA 与 CI；本轮未推送，不能沿用上一提交的 CI 结果。
4. 先设计并实现权益台账与 Drop-in Hold 的数据约束，再接公开交易入口；保留原 Session → Attempt 锁顺序、幂等与防超售。
5. 商品可售验证 → Hold / Cart / Checkout → orders/paid → 权益与确认；补失败、超时、重复和乱序回调测试。
6. 已有 Pass 确认、付款后恢复/Needs Attention；最后启用预约开关并验收完整链路。

## 打开页面

- Home：http://127.0.0.1:9292/#skyra-booking-home
- Programs：http://127.0.0.1:9292/pages/programs#skyra-booking-programs
- App host：http://127.0.0.1:9293/#skyra-booking-home
- 远程开发 Home：https://skyra-booking-dev.myshopify.com/?preview_theme_id=192227082532#skyra-booking-home
- 远程开发 Programs：https://skyra-booking-dev.myshopify.com/pages/programs?preview_theme_id=192227082532#skyra-booking-programs
- Admin：https://admin.shopify.com/store/skyra-booking-dev/apps/skyra-booking/app
- Theme editor：https://admin.shopify.com/store/skyra-booking-dev/themes/192227082532/editor

本地预览也会请求 Shopify，不能离线使用；远程开发主题中的 Booking API 仍依赖本地 App / tunnel。新浏览器可能需要开发店密码。裸店铺根网址显示发布中的 Horizon，不代表正在预览 Booking 开发主题。

开发主题 192227082532；发布中的 Horizon 192196772132。不要误选旧扩展 host 192225313060，也不要发布或替换 Horizon。App 为 Skyra Booking，公开 Client ID 为 c9d266a38e2f11a1240139974253b3a1。

## 本地启动与检查

从 D:\Skyra\booking-app 启动，先检查是否已有同项目服务，避免重复占用端口。

```powershell
# Docker Desktop 已打开时，按项目 compose 配置启动依赖
npm.cmd run preview:app
# 第二个终端
npm.cmd run preview:theme
# 网络恢复后按需执行一次；会请求 Shopify
npm.cmd run preview:check
```

PostgreSQL 17：127.0.0.1:55432，容器 skyra-booking-postgres-1；Redis 7：127.0.0.1:56379，容器 skyra-booking-redis-1。不要停止其他项目容器，不要重置数据库。环境变量/凭据保留在本机，不写入交接或 Git。

测试课：[DEV] Aerial Foundations，2026-09-12 18:30–19:30 Australia/Sydney，Development Coach，容量 8。Session ID：768ac3e9-1a22-4ca2-9616-2433a9dedaee。之后开新会话可能已过期，先检查课表；不要重跑 seed 制造重复数据。

## 验证证据

- npm.cmd run test:db：64 项通过；新增 3 个 Drop-in 数据库/接口测试，覆盖身份隔离、数据注入拒绝、同步/价格/版本与可用性。
- npm.cmd run check：类型、lint、生产构建通过；最终脚本语法与文档完整性在提交前本地检查。
- booking-login-smoke.cjs：11 项本地浏览器 fixture 场景通过，包含 Home/Programs × 320/390/430/1440px 和 3 种模拟登录导航；覆盖 Drop-in 价格刷新/Edit/不可用及原 Recovery。
- Shopify 官方扩展校验：8 文件通过；公共 schema 刷新失败时使用官方缓存，验证遥测关闭。
- 真实 Home 1440/390px 的此前 Browse 检查无横向溢出；Programs HTTP 200/mount 已确认。本轮最终真实双入口完整页面、真实登录及付款未验收。
- 截图/fixture 证据在 output/playwright/booking-login/；真实页面历史证据在 output/playwright/booking-live-*。output 为本地忽略目录，不随 Git 交付。

## 代码入口

- booking-app/app/services/booking.server.ts：Attempt、容量、内部 Hold、pass-options / Drop-in。
- booking-app/theme-extension-src/：booking.js / calendar.js / attempt.js / login.js / transaction.js 源码。
- booking-app/extensions/skyra-booking-embed/assets/：构建产物；改源后运行 npm.cmd run build:theme-extension，不只手改压缩文件。
- booking-app/tests/booking-engine.test.ts 与 scripts/booking-login-smoke.cjs：数据库和浏览器 fixture。
- booking-app/scripts/dev-app.ps1、dev-theme.ps1、preview-health.mjs：启动与诊断。
- docs/booking-system/booking-pass-flow.md、implementation-backlog.md、development-status.md、preview-testing.md：流程、任务、状态与测试说明。

## Git 与保护项

分支 bookingdev；origin 为 https://github.com/robber-857/Skyra.git。
此前已推送：7c3ff67（基础）、02d8294（共享 UI/Review）、743e242（翻周/恢复/诊断）。743e242 的 CI 已通过：https://github.com/robber-857/Skyra/actions/runs/34557206055 。本轮 Drop-in、启动兼容与交接文档仅在本地提交，SHA 用 git log -1 读取；尚未推送或运行其 GitHub CI。

原有 shopify-theme/assets/skyra.css 为无关未提交改动，必须保持且不纳入 Booking 提交。交接复核 SHA256：4FF6A5D8D5C51AA6E158B823EBE9D2BAFA171FACEA1AB17E5B6E150B9E3B657B。

Windows 使用 npm.cmd / npx.cmd。普通工具若报 helper_unknown_error 可按权限流程请求执行；遇到自动审批拒绝时遵守拒绝，不通过其他工具绕过。Shopify API / 平台开发遵循 booking-app/AGENTS.md，先使用官方 AI Toolkit 验证。
