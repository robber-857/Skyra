// Release gates, not environment overrides. Keep closed until paid-webhook,
// customer/payment ownership and expired-hold recovery are fully verified.
export const checkoutAvailable = false;
export const ownedPassesAvailable = false;
